---
import { getCollection } from "astro:content";
import Container from "@components/Container.astro";
import PageLayout from "@layouts/PageLayout.astro";
import ArrowCard from "@components/ArrowCard.astro";
import Link from "@components/Link.astro";
import { dateRange } from "@lib/utils";
import { SITE, HOME, SOCIALS } from "@consts";

const blog = (await getCollection("blog"))
  .filter(post => !post.data.draft)
  .sort((a, b) => b.data.date.valueOf() - a.data.date.valueOf())
  .slice(0,SITE.NUM_POSTS_ON_HOMEPAGE);

const projects = (await getCollection("projects"))
  .filter(project => !project.data.draft)
  .sort((a, b) => b.data.date.valueOf() - a.data.date.valueOf())
  .slice(0,SITE.NUM_PROJECTS_ON_HOMEPAGE);

const allwork = (await getCollection("work"))
  .sort((a, b) => new Date(b.data.dateStart).valueOf() - new Date(a.data.dateStart).valueOf())
  .slice(0,SITE.NUM_WORKS_ON_HOMEPAGE);

const work = await Promise.all(
  allwork.map(async (item) => {
    const { Content } = await item.render();
    return { ...item, Content };
  })
);

---

<PageLayout title="Slyvon Blanco | My home on the internet" description={HOME.DESCRIPTION}>
  <Container>
<section>
    <div class="flex items-center mb-8 py-2">
      <img
        class="w-24 mr-8 animate"
        src="./sly-memoji.svg"
      />
      <div>
        <h1 class="animate font-bold text-black dark:text-white">Hi, I'm Sly</h1>
        <p class="animate text-lg text-gray-700">
          I'm an independent web developer, cybersecurity afficionado, and chicken wings addict
        </p>
      </div>
    </div>
  </section>

    <div class="space-y-16">
      <section>
        <article class="space-y-4">
          <p class="animate">
            I dabble in all things tech and wear many hats, but I spend most of my time <Link href="https://aestheticpixels.io/" aria-label="AestheticPixels.io" external>making websites</Link> for clients or working on side projects like <Link href="https://simplifiedfitness.io/" aria-label="SimplifiedFitness.io" external>SimplifiedFitness.io</Link>. IRL, you can probably find me exploring a new city, at a gym lifting heavy things, or at home rewatching a Christopher Nolan movie.
          </p>
         
          <p class="animate">
            Welcome to my little corner of the internet.
          </p>
        </article>
      </section>

      <section class="animate space-y-6">
        <div class="flex flex-wrap gap-y-2 items-center justify-between">
          <h5 class="font-semibold text-black dark:text-white">
            Latest Posts
          </h5>
          <Link href="/blog">
            View all 
          </Link>
        </div>
        <ul class="flex flex-col gap-3">
          {blog.map(post => (
            <li>
              <ArrowCard entry={post} />
            </li>
          ))}
        </ul>
      </section>


      <section class="animate space-y-4">
        <h5 class="font-semibold text-black dark:text-white">
          Let's Connect
        </h5>
        <article>
          <p>
            If you want to get in touch with me about something or just to say hi,
            feel free to reach out to me on <Link href="https://www.linkedin.com/in/slyvon/" aria-label="LinkedIn" external>LinkedIn</Link>, <Link href="https://www.instagram.com/slyvonblanco" aria-label="Instagram" external>Instagram</Link>, or <Link href="https://x.com/slyvonblanco" aria-label="X" external>X.com</Link>.
          </p>
        </article>
      </section>
    </div>
  </Container>
</PageLayout>
